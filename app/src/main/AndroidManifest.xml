<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.josea.pokefit">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ActividadPrincipal"
            android:configChanges="screenSize|orientation"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ActividadTypes"
            android:label="@string/title_activity_actividad_types"
            android:parentActivityName=".ActividadPrincipal"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.josea.pokefit.ActividadPrincipal" />
        </activity>

        <activity
            android:name=".ActividadVerPokemon"
            android:label="@string/title_activity_actividad_ver_pokemon"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".ActividadAbilities"
            android:label="@string/title_activity_actividad_abilities"
            android:parentActivityName=".ActividadPrincipal"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.josea.pokefit.ActividadPrincipal" />
        </activity>

    </application>

</manifest>